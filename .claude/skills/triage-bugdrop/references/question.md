# Question (`question` label): answer it

1. Skip if there's a `triage-bugdrop:answer` comment and no newer human comment after it. If there is a newer human comment, answer that follow-up.
2. Answer from this repo only: `README.md`, `docs/`, and the code. Cite file paths.
3. If it can't be answered from the repo, or isn't about this project, say so plainly instead of guessing.
4. Post the answer as a comment starting with `<!-- triage-bugdrop:answer -->`. Leave the issue open for the reporter.
